# cómo paso datos del frontmatter al <script></script>
## R: Files locales, solo se llaman desde el script. Otros files se pueden CREO del Frontmatter.
    1. we need a way to store our variables and then read them when JavaScript runs in the browser.

    2. use data-* attributes to store the value of variables in your HTML output. Scripts, including custom elements, can then read these attributes using an element’s dataset property once your HTML loads in the browser.


#